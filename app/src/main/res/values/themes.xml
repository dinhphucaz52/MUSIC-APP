<resources>

    <style name="Base.Theme.MyMusicApp" parent="Theme.MaterialComponents.NoActionBar">

    </style>

    <style name="Theme.MyMusicApp" parent="Base.Theme.MyMusicApp" />

    <style name="Theme.MyMusicApp.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.MyMusicApp.AppWidgetContainer" parent="Theme.MyMusicApp.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
        <item name="appWidgetPadding">16dp</item>
    </style>
</resources>